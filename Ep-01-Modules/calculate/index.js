const {calculateMultiply} = require("./multiply");
const {calculateSum} = require("./sum");

module.exports = {
    calculateSum,
    calculateMultiply
}

// this way we can export multiple modules together